<script type="text/javascript">
  @{  int a = ViewBag.Message; }
    if (@a == 0) {
        alert("用户或密码错误");
    }
    if (@a== -1) {
        ;
    }
    if (@a== 1) {
        alert("登录成功,你可以借书或还书！");
      window.location.href ="@Url.Action("Borrow","Home")";
    }
</script>
@using (Html.BeginRouteForm("Default", new { controller = "Home", action = "User", Id = 100 }, FormMethod.Post))
{
    <p>名字：</p> <input type="text" name="name" />
    <p>密码：</p><input type="text" name="password" />
    <input type="submit" value="普通用户登录" class="btn btn-default" />
}